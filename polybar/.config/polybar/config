; ____   ___  _  __   ______    _    ____  
;|  _ \ / _ \| | \ \ / / __ )  / \  |  _ \ 
;| |_) | | | | |  \ V /|  _ \ / _ \ | |_) |
;|  __/| |_| | |___| | | |_) / ___ \|  _ < 
;|_|    \___/|_____|_| |____/_/   \_\_| \_\
[colors]
;background =#bfb7b7
;bar colour
;background-alt =#ef1717
;highlighted
;foreground =#000000
;text
;foreground-alt = #6a6a6a
;primary = #f21010
;secondary = #52b030
;alert = #a54242
[colors]
background = ${xrdb:color0:#222}
background-alt= ${xrdb:color4:#222}
foreground = ${xrdb:color7:#222}
foreground-alt = ${xrdb:color7:#222}
primary = ${xrdb:color1:#222}
secondary = ${xrdb:color2:#222}
alert = ${xrdb:color3:#222}


[bar/top]
radius = 5
border-size = 10
border-color = #00000000
width = 100%
height = 25
fixed-center = true

background = ${colors.background}
foreground = ${colors.foreground}

line-size = 3
line-color = #a7adba

padding-left = 1
padding-right = 1
padding-top = 1

module-margin-left = 2
module-margin-right = 1

;font-0= Inconsolata :size=8;1
font-0 = "M+ 1mn:size=8;0"
font-1 = "Material Design Icons:size=9;1"
font-2= Weather Icons:size=8;1
;font-2 = "font\-logos:size=11;1"
font-3 = fixed:pixelsize=8;1

modules-left = xworkspaces 
modules-center = info-kernel
modules-right = filesystem cpu updates-arch-combined popup-calendar pulseaudio battery-combined-shell wired-network wireless-network

;tray-position =left
;tray-maxsize = 30
;tray-scale =1.0

cursor-click = pointer
cursor-scroll = ns-resize

wm-restack = i3
override-redirect = false

[module/title]
type = internal/xwindow
label =  %title% 
label-maxlen = 50
format-background = ${colors.background-alt}
format-foreground = ${colors.foreground}
format-padding = 2

[module/spotify]
type = custom/script
interval = 1
format-prefix = " "
format = <label>
exec = python /home/reueljohn/.config/polybar/spotify_status.py -f '{artist}: {song}' 
format-underline = ${colors.background-alt} 

[module/mpd]
type = internal/mpd
format-online = <icon-prev> <toggle> <icon-next> <label-song>
label-maxlen = 30

icon-play = 
icon-pause = 
icon-stop = 
icon-prev = 
icon-next = 

bar-progress-width = 10
bar-progress-indicator = |
bar-progress-fill = ─


host = 127.0.0.1
port = 6600

; Seconds to sleep between progressbar/song timer sync
; Default: 1
interval = 2

[module/openweathermap-simple]
type = custom/script
exec =/home/reueljohn/.config/polybar/polybar-scripts/polybar-scripts/openweathermap-simple/openweathermap-simple.sh 
interval = 600
label-font = 3
format-underline = ${colors.background-alt}

[module/info-kernel]
type = custom/script
exec = uname -r
format-prefix = "  "
format-underline = ${colors.background-alt}
interval = 1024

[module/cpu]
type = internal/cpu

; Seconds to sleep between updates
; Default: 1
interval = 10
; Available tags:
;   <label> (default)
;   <bar-load>
;   <ramp-load>
;   <ramp-coreload>
format = <label> 
format-underline = ${colors.background-alt}
; Available tokens:
;   %percentage% (default) - total cpu load
;   %percentage-cores% - load percentage for each core
;   %percentage-core[1-9]% - load percentage for specific core
label =  %percentage%%

[module/openweathermap-detailed]
type = custom/script
exec = /home/reueljohn/.config/polybar/polybar-scripts/polybar-scripts/openweathermap-detailed/openweathermap-detailed.sh
interval = 600
label-font = 3
format-underline = ${colors.background-alt}

[module/filesystem]
type = internal/fs

; Mountpoints to display
mount-0= /home

; Seconds to sleep between updates
; Default: 30

; Display fixed precision values
; Default: false
fixed-values = true
label-mounted = %free% 
; Spacing between entries
; Default: 2
spacing = 4
format-mounted-underline = ${colors.background-alt}

[module/updates-arch-combined]
type = custom/script
exec = /home/reueljohn/.config/polybar/polybar-scripts/polybar-scripts/updates-arch-combined/updates-arch-combined.sh
interval = 600
format-underline = ${colors.background-alt}

[module/pulseaudio]
type = internal/pulseaudio

; Sink to be used, if it exists (find using `pacmd list-sinks`, name field)
; If not, uses default sink
sink = alsa_output.pci-0000_12_00.3.analog-stereo

; Use PA_VOLUME_UI_MAX (~153%) if true, or PA_VOLUME_NORM (100%) if false
; Default: true
use-ui-max = true

; Interval for volume increase/decrease (in percent points) (unreleased)
; Default: 5
interval = 5

; Available tags:
;   <label-volume> (default)
;   <ramp-volume>
;   <bar-volume>
format-volume =  <label-volume>
; Available tags:
;   <label-muted> (default)
;   <ramp-volume>
;   <bar-volume>
;format-muted = <label-muted>

; Available tokens:
;   %percentage% (default)
;label-volume = %percentage%%

; Available tokens:
;   %percentage% (default)
label-muted = 🔇 muted
label-muted-foreground = #666
format-volume-underline = ${colors.background-alt}

[module/popup-calendar]
type = custom/script
exec = /home/reueljohn/.config/polybar/polybar-scripts/polybar-scripts/popup-calendar/popup-calendar.sh
interval = 5
click-left = ~/polybar-scripts/popup-calendar.sh --popup
format-underline = ${colors.background-alt}

[module/wired-network]
type = internal/network
interface = enp1s0
label-disconnected-foreground = ${colors.foreground-alt}
label-connected =  %local_ip%
format-connected-underline = ${colors.background-alt}

[module/wireless-network]
type = internal/network 
interface = wlp2s0
label-disconnected-foreground = ${colors.foreground-alt}
label-connected = %essid% 
label-disconnected = 
format-connected-underline = ${colors.background-alt}

[module/battery-combined-shell]
type = custom/script
exec = /home/reueljohn/.config/polybar/polybar-scripts/polybar-scripts/battery-combined-shell/battery-combined-shell.sh
interval = 5
format-underline = ${colors.background-alt}

[module/xworkspaces]
type = internal/xworkspaces
icon-0 = 1;
icon-1 = 2;
icon-2 = 3;
icon-3 = 9;
icon-4 = 10;
icon-default = 

format = <label-state>

; Only show workspaces on the same output as the bar
pin-workspaces = false

; focused = Active workspace on focused monitor
label-active = %icon%
label-active-foreground = ${colors.foreground}
label-active-background = ${colors.background-alt}
label-active-padding = 2

; occupied = Inactive workspace on any monitor
label-occupied = %icon%
label-occupied-padding = 2

; urgent = Workspace with urgency hint set
label-urgent = %icon%
label-urgent-background = ${colors.alert}
label-urgent-padding = 2

label-empty = %icon%
label-empty-foreground = ${colors.foreground}
label-empty-padding = 2

[settings]
screenchange-reload = true
;compositing-background = xor
;compositing-background = screen
;compositing-foreground = source
;compositing-border = over

[global/wm]
margin-top = 5
margin-bottom = 5

; vim:ft=dosini
